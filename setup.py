#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='read2text',
    version='0.0.1',
    url='',
    license='',
    packages=find_packages(),
    author='Brett Terpstra',
    author_email='',
    description='read2text',
    include_package_data=True,
    install_requires=[
    ],
)
